# CI/CD 初版實作方案（FastAPI + React LIFF + n8n）

> 部署環境：學校實驗室 Ubuntu VM（k3s）
> 部署方式：Docker + Kubernetes + NGINX Ingress
> CI/CD：GitHub Actions + SonarCloud
> 映像倉庫：Docker Hub
> 目標：每次推送到 `main` 後，自動測試、分析、建置並部署到 Kubernetes 測試環境，使用 NGINX Ingress 作為統一入口。
> VM IP：140.121.196.16

---

# 0. k3s 前置設定（Ubuntu VM）

```bash
curl -sfL https://get.k3s.io | sh -s - --disable traefik
sudo kubectl get nodes
```

說明：

* k3s 預設會安裝 Traefik。若要使用 ingress-nginx，建議安裝時關閉 Traefik。
* n8n PVC 建議使用 k3s 預設 `local-path` StorageClass。

---

# 1. 系統組成

* 前端：LIFF（React + TypeScript），以 NGINX 容器提供靜態檔
* 後端：FastAPI
* 工作流程：n8n
* 容器化：Docker
* 編排：Kubernetes（k3s）
* Ingress Controller：[NGINX Ingress Controller](https://kubernetes.github.io/ingress-nginx/?utm_source=chatgpt.com)
* CI/CD：[GitHub Actions](https://github.com/features/actions?utm_source=chatgpt.com)
* 程式碼品質：[SonarCloud](https://sonarcloud.io?utm_source=chatgpt.com)

---

# 2. 整體架構

```mermaid
flowchart TB
    Developer --> GitHub
    GitHub --> Actions[GitHub Actions]
    Actions --> Sonar[SonarCloud]
    Sonar --> Gate{Quality Gate}
    Gate -->|Pass| Registry[Docker Hub]
    Gate -->|Fail| Stop[停止部署]

    Registry --> K8s[k3s Cluster]

    Internet --> NginxIngress[NGINX Ingress Controller]

    NginxIngress --> FrontendSvc[care-frontend]
    NginxIngress --> BackendSvc[care-backend]
    NginxIngress --> N8nSvc[n8n-service]

    FrontendSvc --> FrontendPod[React + NGINX]
    BackendSvc --> BackendPod[FastAPI]
    N8nSvc --> N8nPod[n8n]
```

---

# 3. Port 規劃

## VM 對外 Port

| Port | 用途 | 說明 |
| ---: | --- | --- |
| 80 | HTTP | 對外入口（開發階段） |
| 443 | HTTPS | 未來正式環境使用 |
| 22 | SSH | 系統維護 |

## Ingress 路由

| 路徑 | 服務 |
| --- | --- |
| `/` | React LIFF |
| `/api` | FastAPI |
| `/n8n` | n8n |

---

# 4. Container Port

| 服務 | Container Port |
| --- | ---: |
| React + NGINX | 80 |
| FastAPI (Uvicorn) | 8000 |
| n8n | 8100 |

---

# 5. Kubernetes Service Port

| Service | Port | targetPort |
| --- | ---: | ---: |
| care-frontend | 80 | 80 |
| care-backend | 8000 | 8000 |
| n8n-service | 8100 | 8100 |

---

# 6. 建議網域

| URL | 用途 |
| --- | --- |
| `https://project.example.com/` | LIFF 前端 |
| `https://project.example.com/api` | FastAPI API |
| `https://project.example.com/n8n` | n8n 控制台 |

如果目前只有測試環境，也可以先用：

* `http://<VM_IP>/`
* `http://<VM_IP>/api`
* `http://<VM_IP>/n8n`

---

# 7. GitHub Actions CI/CD 流程

```mermaid
flowchart LR
    Push[Push to main]
    Validate[Render & Validate K8s Manifests]
    Deploy[kubectl apply -k]

    Push --> Validate --> Deploy
```

---

# 8. GitHub Secrets / Variables

## Secrets

* `KUBE_CONFIG_DATA`：`~/.kube/config` 的 base64 字串，供 GitHub Actions 連入 k3s

若應用 repo 也在同一個 workflow 建置映像，另外需要：

* `DOCKERHUB_USERNAME`
* `DOCKERHUB_TOKEN`

## Variables

* `BACKEND_IMAGE_TAG`（例如 `main` 或固定版本）
* `FRONTEND_IMAGE_TAG`（例如 `main` 或固定版本）
* `N8N_IMAGE_TAG`（建議固定版本，不建議長期使用 `latest`）

---

# 9. Kubernetes Namespace

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: care-dev
```

---

# 10. Replicas（Pod 的數量）

| 服務 | Replicas |
| --- | ---: |
| frontend | 1 |
| backend | 1 |
| n8n | 1 |

---

# 11. Persistent Storage

n8n 需要 Persistent Volume Claim（PVC）保存：

* Workflow
* Credentials
* Execution History

建議容量：5-10 GB。

建議設定：

* `storageClassName: local-path`
* `accessModes: [ReadWriteOnce]`

---

# 12. Resource 建議

| 服務 | CPU | Memory |
| --- | ---: | ---: |
| frontend | 100m | 128Mi |
| backend | 500m | 512Mi |
| n8n | 1000m | 1Gi |

---

# 13. Repository 結構

建議將三個服務與 infra 分成四個 repo：

* care-backend
* care-liff
* care-n8n
* care-infra

---

# 14. 部署觸發條件

| Branch | 行為 |
| --- | --- |
| `feature/*` | manifest validate 與品質檢查 |
| `main` | 自動部署到 k3s VM |

---

# 15. 正式環境未來可擴充

* Kong
* Prometheus + Grafana
* TLS（cert-manager + Let's Encrypt）

---

# 16. 最終部署網址

| 功能 | URL |
| --- | --- |
| LIFF 前端 | `http://<VM_IP>/` |
| FastAPI | `http://<VM_IP>/api` |
| n8n | `http://<VM_IP>/n8n` |

---

# 17. 參考資料

https://learn.microsoft.com/zh-tw/azure/databricks/dev-tools/ci-cd/best-practices
