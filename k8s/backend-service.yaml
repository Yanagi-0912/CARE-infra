# Service：提供叢集內穩定入口，將流量導到 care-backend Pod。
apiVersion: v1
kind: Service
metadata:
  name: care-backend
  namespace: care-dev
  labels:
    app: care-backend
spec:
  type: ClusterIP
  selector:
    app: care-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
