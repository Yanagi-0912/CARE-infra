apiVersion: v1
kind: Service
metadata:
  name: n8n-service
  namespace: care-dev
  labels:
    app: n8n
spec:
  type: ClusterIP
  selector:
    app: n8n
  ports:
    - name: http
      port: 8100
      targetPort: 8100