apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data
  namespace: care-dev
  labels:
    app: n8n
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi